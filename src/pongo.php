<?php

use Pongo\Cms\Services\Validators\PongoValidator;

/**
 * Instantiate CustomValidator class
 */
/*Validator::resolver(function($translator, $data, $rules, $messages)
{
	$page = App::make(Config::get('cms::system.repositories.page.interface'));
	$element = App::make(Config::get('cms::system.repositories.element.interface'));

	return new PongoValidator($translator, $data, $rules, $messages, $page, $element);
});*/